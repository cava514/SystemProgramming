<Window x:Class="LabWork27.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork27"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <StackPanel>
        <Label Content="Ваше имя"/>
        <TextBox Name="NameUserTextBox"/>
        <Button Name="InputNameUserButton" Content="Ввести имя" Click="InputNameUserButton_Click"/>
        <Grid Name="ChatHubStackPanel" IsEnabled="False">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Content="Сообщение" Grid.Row="0" Grid.ColumnSpan="2"/>
            <TextBox Name="MessageTextBox" Grid.Row="1" Grid.ColumnSpan="2"/>
            <Button Name="InputMessageButton" Content="Ввести сообщение" Click="InputMessageButton_Click" Grid.Row="2" Grid.ColumnSpan="2"/>
            <ListBox Name="GetChatHubListBox" Grid.Row="3" Grid.Column="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding }"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <ListBox Name="SendChatHubListBox" Grid.Row="3" Grid.Column="1">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding }"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </StackPanel>
</Window>
